let kids = [
    { first : "Natalie", last : "Plyers" },
     { first: "Brittany", last: "Ray" },
     { first: "Zachary", last: "Westly" }
    ];

   // let lenght = kids.lenght;
   // for (let i=0 ; i < lenght ; i++ ){
  //      console.log("the name of the child is " + chils[1].first + " " + child[1].last)
  //  }


    ///for (let kid of kid ){
   //     console.log("the name of the child is " + chils.first + " " + child.last)

    //}

    /////
    kids.foreach(display.child)

    function displayChild(child){
        console.log( 'the name of the child is ${childs.first} ${childs.last} ');
    }

    

